<section class="about-brand">
  <div class="container">
    <div class="about-brand-inner" data-animate>
      <span class="section-label">About SEO Brothers</span>
      <h2 class="section-heading">
        A Local SEO Company,<br />
        Built by the Bate Brothers
      </h2>
      <p class="about-brand-text">
        Devon and Adam Bate started SEO Brothers in 2015 and have been doing the
        work ever since: ranking local and service businesses in the markets
        where being found is the difference between a quiet phone and a full
        calendar. Peter joined in 2019, Colin in 2025, and today a team of
        thirty plus runs campaigns from our Halifax, Nova Scotia headquarters.
      </p>

      <div class="about-brand-facts">
        <div class="about-brand-fact">
          <span class="about-brand-fact-label">Founded</span>
          <span class="about-brand-fact-value">2015</span>
        </div>
        <div class="about-brand-fact">
          <span class="about-brand-fact-label">Headquarters</span>
          <span class="about-brand-fact-value">Halifax, NS</span>
        </div>
        <div class="about-brand-fact">
          <span class="about-brand-fact-label">Founders</span>
          <span class="about-brand-fact-value">Devon & Adam Bate</span>
        </div>
        <div class="about-brand-fact">
          <span class="about-brand-fact-label">Focus</span>
          <span class="about-brand-fact-value">Local & service business SEO</span>
        </div>
      </div>

      <a href="/about/" class="about-brand-link">
        Read the story
        <svg width="14" height="14" viewBox="0 0 16 16" fill="none">
          <path
            d="M3 8h10M9 4l4 4-4 4"
            stroke="currentColor"
            stroke-width="1.5"
            stroke-linecap="round"
            stroke-linejoin="round"></path>
        </svg>
      </a>
    </div>
  </div>
</section>

<style>
  .about-brand {
    padding: 100px 0;
    background: var(--white);
    border-bottom: 1px solid var(--border-light);
  }

  .about-brand-inner {
    max-width: 820px;
    margin: 0 auto;
    text-align: center;
  }

  .about-brand-inner .section-label {
    display: inline-block;
  }

  .about-brand-text {
    font-size: 18px;
    line-height: 1.7;
    color: var(--text-secondary);
    margin: 8px auto 40px;
    max-width: 680px;
  }

  .about-brand-facts {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 24px;
    margin-bottom: 40px;
    padding: 28px 0;
    border-top: 1px solid var(--border-light);
    border-bottom: 1px solid var(--border-light);
  }

  .about-brand-fact {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 6px;
    text-align: center;
  }

  .about-brand-fact-label {
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--text-muted);
  }

  .about-brand-fact-value {
    font-family: var(--font-display);
    font-size: 16px;
    font-weight: 600;
    color: var(--text-primary);
    line-height: 1.3;
  }

  .about-brand-link {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 15px;
    font-weight: 600;
    color: var(--teal);
    transition: gap var(--transition);
  }

  .about-brand-link:hover {
    gap: 10px;
  }

  @media (max-width: 768px) {
    .about-brand {
      padding: 72px 0;
    }

    .about-brand-facts {
      grid-template-columns: 1fr 1fr;
      gap: 20px 16px;
    }
  }

  @media (max-width: 420px) {
    .about-brand-facts {
      grid-template-columns: 1fr;
    }
  }
</style>
